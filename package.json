{
  "name": "ucns-test-cci",
  "version": "0.1.0",
  "scripts": {
    "lint": "eslint src/**/*.js __tests__/**/*.js",
    "lint-ci": "eslint src/**/*.js __tests__/**/*.js --format junit --output-file reports/eslint-results.xml",
    "test": "jest",
    "test-ci": "jest --runInBand",
    "test-ci-coverage": "jest --runInBand --collectCoverage=true"
  },
  "devDependencies": {
    "eslint": "^8",
    "eslint-config-google": "latest",
    "eslint-plugin-jest": "^27",
    "jest": "^29",
    "jest-junit": "^16",
    "suitecloud-unit-testing-stubs": "^1",
    "@oracle/suitecloud-unit-testing": "^1",
    "@oracle/suitecloud-cli": "^1",
    "@types/jest": "^29"
  }
}
